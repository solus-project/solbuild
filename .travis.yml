language: go

dist: trusty
sudo: false

addons:
  apt:
    packages:
      - libgit2-dev
go:
  - 1.6
  - 1.7.x
  - tip

before_script:
  - go get -u github.com/client9/misspell/cmd/misspell

script:
  - make ensure_modules
  - make compliant && git diff --exit-code
  - make
  - make check
